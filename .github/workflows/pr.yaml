name: hello-world

on:
  push:
    branches:
      - qa

jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Printing with Python
        run: print("Hello, World!")
        shell: python
