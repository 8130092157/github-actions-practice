name: git-cli

on:
  push:

jobs:
  cli:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{secrets.GITHUB_TOKEN}}
    steps:
      - uses: actions/checkout@v4
      - run: env
      - run: gh auth status
