name: Print a greeting message

permissions:
  contents: read
  pull-requests: write

on: push

jobs:
  print-greeting:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - use: ./
        with:
          name: "Abhay"

